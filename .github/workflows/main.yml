name: CI

on: [push]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: echo Installing zig
    - run: npm install zig
  
  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: echo Testing 
    - run: ./tests/main.py
